/**
 * Realistic database seeding is intentionally disabled.
 * Only genuine database records created by users and administrators are used.
 */

const seedRealData = async () => {
  console.log('ℹ️ Mock/Dummy data seeding is disabled. Live database records are preserved.');
};

module.exports = seedRealData;
