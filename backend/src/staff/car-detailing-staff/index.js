const routes = require('./carDetailingStaff.routes');
const controller = require('./carDetailingStaff.controller');
const service = require('./carDetailingStaff.service');
const model = require('./carDetailingStaff.model');
const middleware = require('./carDetailingStaff.middleware');

module.exports = {
  routes,
  controller,
  service,
  model,
  middleware
};
