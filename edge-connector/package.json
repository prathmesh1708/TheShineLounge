{
  "name": "tsl-edge-connector",
  "version": "1.0.0",
  "private": true,
  "description": "On-site bridge between The Shine Lounge cloud and the ANPR / tunnel wash hardware",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "simulate": "node src/simulator.js",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "engines": { "node": ">=18" }
}
